# Public host port where you access the app on the server.
HOST_PORT=8501
# Container/application port used by Streamlit.
PORT=8501
# Keep clean export disabled by default for privacy.
SERO_ALLOW_CLEAN_EXPORT=0
# Optional override for anonymization model.
# SERO_NER_MODEL=Davlan/bert-base-multilingual-cased-ner-hrl
